name: CloudFormation CI

on:
  push:
    branches:
      - main
      - develop
      - "feature/**"
    paths:
      - "infrastructure/cloudformation/**"
      - ".github/workflows/cloudformation-ci.yml"
  pull_request:
    branches:
      - main
      - develop
    paths:
      - "infrastructure/cloudformation/**"
      - ".github/workflows/clou**ormation-ci.yml"
  workflow_dispa**h:

permissions:
  contents: read**jobs:
  validate-cloudformation:
**  name: Validate CloudFormation
 ** runs-on: ubuntu-latest

    step**
      - name: Check out reposito**
        uses: actions/checkout@v**
      - name: Set up Python
    **  uses: actions/setup-python@v7
 **     with:
          python-versi**: "3.12"
          cache: pip
   **     cache-dependency-path: infra**ructure/cloudformation/requiremen**.txt

      - name: Install infra**ructure validation tools
        **n: |
          python -m pip inst**l --upgrade pip
          python ** pip install -r infrastructure/cl**dformation/requirements.txt

    **- name: Validate CloudFormation t**plates
        env:
          AWS**EGION: eu-west-2
        run: ./i**rastructure/cloudformation/script**validate.sh
